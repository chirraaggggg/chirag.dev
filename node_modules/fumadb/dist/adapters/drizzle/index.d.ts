import { P as Provider, F as FumaDBAdapter } from '../../index-DG1OqO_B.js';
import '../../index-DHGqocp-.js';
import '../../query/index.js';
import 'kysely';

interface DrizzleConfig {
    /**
     * Drizzle instance, must have query mode configured: https://orm.drizzle.team/docs/rqb.
     */
    db: unknown;
    provider: Exclude<Provider, "cockroachdb" | "mongodb" | "mssql" | "convex">;
}
declare function drizzleAdapter(options: DrizzleConfig): FumaDBAdapter;

export { type DrizzleConfig, drizzleAdapter };
