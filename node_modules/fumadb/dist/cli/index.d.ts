import { FumaDB } from '../index.js';
import '../index-DHGqocp-.js';
import '../index-DG1OqO_B.js';
import '../query/index.js';
import 'kysely';

declare function createCli(options: {
    db: FumaDB<any>;
    /**
     * CLI command name, must be lowercase without whitespaces.
     */
    command: string;
    description?: string;
    /**
     * CLI Version
     */
    version: string;
}): {
    main(): Promise<void>;
};

export { createCli };
