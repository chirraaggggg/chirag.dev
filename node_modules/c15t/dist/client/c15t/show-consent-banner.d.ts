import type { ShowConsentBannerResponse } from '../client-interface';
import type { FetchOptions, ResponseContext } from '../types';
import type { FetcherContext } from './fetcher';
/**
 * Provides offline mode fallback for showConsentBanner API.
 * Simulates the behavior of OfflineClient when API requests fail.
 * @internal
 */
export declare function offlineFallbackForConsentBanner(options?: FetchOptions<ShowConsentBannerResponse>): Promise<ResponseContext<ShowConsentBannerResponse>>;
/**
 * Checks if a consent banner should be shown.
 * If the API request fails, falls back to offline mode behavior.
 */
export declare function showConsentBanner(context: FetcherContext, options?: FetchOptions<ShowConsentBannerResponse>): Promise<ResponseContext<ShowConsentBannerResponse>>;
//# sourceMappingURL=show-consent-banner.d.ts.map